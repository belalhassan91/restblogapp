<%- include('./partials/header.ejs'); %>
<div class="ui container">
    <h1>INDEX PAGE</h1>
    <% blogs.forEach(blog => { %>
        <div>

            <h2><%= blog.title%></h2>
            <img src="<%= blog.image %>">
            <span><%= blog.created%></span>
            <p><%= blog.body%></p>
        </div>
    <% }); %>
</div>
<%- include('./partials/footer.ejs'); %>